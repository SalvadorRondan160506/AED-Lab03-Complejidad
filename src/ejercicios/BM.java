package ejercicios;
/*
public class BM {
    FUNCIÓN BM (v : VECTOR(ENTERO), n : ENTERO) : ENTERO
        m := v[0]                    // O(1)
        PARA i DESDE 1 HASTA n-1    // n-1 iteraciones
            SI v[i] > m              // O(1)
            m := v[i]            // O(1)
        FINSI
        FINPARA                      // (n-1) × O(1) = O(n)
        DEVOLVER m                   // O(1)
    FINFUNCION

}

//complejidad final O(n)
 */